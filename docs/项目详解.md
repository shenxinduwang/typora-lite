# Typora-Lite 项目详解

> 本文件承载 README 之外的深度内容：**方案选型、完整功能清单与已知边界、桌面集成详解、观感优化路线决策、版本路线与轮次记录、借鉴 Telari 的功能与思路路线**。首页请看 [README](../README.md)，逐轮踩坑与解法见 [开发日志](开发日志.md)。

## 1. 方案选型

核心诉求：原生、体积小、冷启动快、Typora 式实时渲染、仅 Windows。

| 方案 | 体积(打包后) | 冷启动 | Typora 式实时渲染 | 开发成本 | 结论 |
|---|---|---|---|---|---|
| **Tauri 2 + WebView2 + CodeMirror 6**（本仓库采用） | ~5–10 MB | 极快(<300ms) | ✅ CM6 装饰实现打字即渲染 | 中 | **推荐**：二进制小、用系统 WebView2 无需自带浏览器内核 |
| Electron | ~150 MB+ | 慢(自带 Chromium) | ✅ | 低 | ❌ 体积/冷启动不符合 |
| C# WPF + WebView2 | ~30 MB(自包含) | 快 | ✅ 同 CM6 | 中 | 备选，团队熟 .NET 时可用 |
| C++/C# + Scintilla(RichEdit) | 极小 | 极快 | ❌ 只能源码或双栏 | 高 | ❌ 做不到 Typora 式原地渲染 |
| Qt(Rust CXX) | ~40 MB | 快 | ❌ 需自研富文本 | 很高 | ❌ |
| Rust 纯原生(egui/iced) | 小 | 快 | ❌ Markdown 排版需自研 | 很高 | ❌ |

**为什么是 Tauri + WebView2**：Typora 本身就是 Electron(Chromium)，体积和冷启动是其被吐槽的点。Tauri 复用 Windows 自带的 **WebView2（Edge 内核）**，所以安装包只有几 MB、启动没有自带 Chromium 的加载开销；富文本渲染交给前端 **CodeMirror 6**，用「装饰(Decoration)」把 `#`/`**`/`` ` `` 等标记原地隐藏并即时套用样式，得到 Typora 手感。

## 2. 完整功能清单

### 已实现

- **查找 / 替换（Ctrl+F）**：`@codemirror/search` 接入，搜索面板置顶（含替换/全部替换）、全部匹配高亮、`F3`/`Shift+F3` 上下导航、`Mod-D` 选中同词；选中的词自动高亮同处出现。
- 新建 / 打开 / 保存 `.md`，快捷键 `Ctrl+S/O/N`，撤销重做、括号自动配对。
- **大纲侧栏（长文目录）**：工具栏「大纲」按钮或 `Ctrl+Shift+O` 开关；提取全文档 H1–H6（含 Setext）生成目录树，点击跳转 + 滚动定位，跟随光标高亮当前章节。
- **未保存关窗拦截**：点 × / Alt+F4 且有改动时弹原生确认（保存并退出 / 放弃修改 / 取消），保存失败（只读盘等）会留在编辑器；浏览器兜底用 `beforeunload` 拦截。切换文档同样有 confirm 拦截。
- **编码兼容（Windows 刚需）**：UTF-8 / UTF-8 BOM / UTF-16LE/BE / **GBK** 自动识别（`encoding_rs`），**保存按原编码写回**（BOM 补回），GBK 中文 .md 不再"双击没反应"；BOM 剥离后首行标题正常渲染。
- **原子保存**：同目录临时文件 + rename 覆盖（Windows 即 `MoveFileEx(REPLACE_EXISTING)`），写一半崩溃不损坏原文件。
- **打开/保存失败弹原生错误框**：不再只进 console 表现为"点了没反应"。
- **撤销历史与文件切换隔离**：载入/新建走 `EditorState` 整体重建，Ctrl+Z 不会"撤销回上一个文件"。
- **纯浏览器亦可运行**：无 Tauri 时用文件选择器打开（TextDecoder 按 BOM/GBK 解码）、Blob 下载保存（UTF-8，BOM 按原文件补回）；在 Tauri 内走原生命令（`IS_TAURI` 自动切换）。
- **未保存脏标记**：LF 归一快照精确比较（CRLF 文件打开即脏的误报已修），标题栏 / 文件名后加 `•`；标题变化才走一次 IPC。
- **最近文件 + 启动恢复**：记录最近 10 个文件（工具栏「最近」下拉直达、可清空）；无启动参数时自动恢复上次文档（文件已删除则静默剔除）。
- **阅读位置记忆**：按文档路径记住光标与滚动位置（localStorage，LRU 上限 50 篇），重新打开时自动跳回上次阅读位置并轻提示「已跳转到上次阅读位置」；跳转顺序先落光标再双 rAF 覆盖滚动（规避 CM 选区自动滚动覆盖）；关窗前强制落盘，位置一律钳制越界（建议值语义）。
- **自动保存 + 崩溃恢复**：脏文档每 3s 写本地草稿，崩溃/误关后下次启动询问恢复；「自动保存」开关开启后每 30s 静默保存到原文件（IME 合成期跳过）。
- **外部文件修改监听**：3s 轮询 mtime，文档干净时自动重载，有本地改动时询问是否丢弃重载。
- **图片粘贴 / 拖入**：截图直接 Ctrl+V、图片文件拖进窗口，自动存到文档同目录 `assets/` 并插入相对路径引用（文档未保存时用 data URL）；浏览器模式插入 data URL。
- **CSP 收紧**：`default-src 'self'` 白名单策略（样式放行内联供 CM6 注入、`img-src data:` 供 data URL 图片、`connect-src ipc:` 供 Tauri IPC），不再依赖 `csp: null`。
- **图片预览（WYSIWYG）**：光标不在 `![alt](src)` 内时渲染为真 `<img>`（相对路径经 Rust 转 base64 data URL，带缓存与加载/失败态样式），光标进入显示源码。
- **链接折叠 + Ctrl+Click 打开**：光标不在链接内时只显示链接文字（URL/括号折叠），独立 URL 自动上色；Ctrl/Cmd+点击用系统默认程序打开（http/https/mailto 白名单）。
- **GBK 保存保护**：文档含 GBK 无法表示的字符（emoji/生僻字）时拒绝静默写乱码，弹窗引导一键改存 UTF-8。
- **外部重载保视图**：外部修改触发的同文件重载保留光标与滚动位置（普通 dispatch，不清撤销栈）；跨文件载入仍走整体重建。
- **暗色全套**：查找面板、tooltip、补全框跟随主题变量，暗色下不再白底。
- **代码块语法高亮**：`@codemirror/language-data` 按 fenced code 语言标签自动着色（js/py/rust/…，懒加载按需解析）。
- **拖放 .md 打开**：把 .md/.markdown/.txt 拖进窗口即打开（Tauri 走原生 `onDragDropEvent`，浏览器走 HTML5 drop），带脏确认与编码识别。
- **列表编辑手感**：回车自动续接列表（有序递增、任务项重置为 `[ ]`、空项回车退出、引用延续，`lang-markdown` 内建）+ `Tab`/`Shift+Tab` 列表缩进（自研 `listEditing.js`，支持多行选区）。
- Typora 式原地渲染：标题、粗体 / 斜体、行内代码、代码块（`FencedCode`）、引用块、链接、**分割线（widget 渲染成真横线）**、删除线。
- **任务列表可点击切换**：`[ ]` / `[x]` 原地渲染成复选框 widget，点一下即在源码里互转；光标进入该项时恢复显示原始 `[ ]` 便于手改。（`mousedown` 用 `posAtDOM` 从被点 DOM 反查精确位置——`posAtCoords` 对无文本宽度的 replace widget 会跨行错位。）
- **列表符 / 表格源码态渲染**：列表标记 `ListMark` 高亮（光标在项内时显示原文 `-`/`1.`）、表头行 `TableHeader` 加粗、表格分隔符 `|` 弱化（光标进表格显示源码时生效）。
- **亮 / 暗主题**：所有颜色走 CSS 变量；默认跟随系统（`matchMedia` 实时翻转），「主题」入口可手动循环 自动/亮/暗，选择持久化（`data-theme` 属性驱动）。
- **已启用 GFM**（`@lezer/markdown` 的 `GFM` 传入 `markdown({extensions})`）：表格、任务列表、删除线、自动链接均能被解析成语法节点（Table/TableCell、Task/TaskMarker、Strikethrough）。
- **光标聚焦才显示标记**：光标离开某段时 `# ** \` > ~~` 自动隐藏，光标移入即重新显示——真正的 Typora 折叠体验。
- 同一行叠加多种行样式（如「引用块里的标题」`> # x`）已做**按行聚合**，不会因重复行装饰导致渲染层报错白屏。
- **中文 IME 合成期不重建装饰**（`u.view.composing` 守卫），避免候选词被打断掉字。
- **代码块真容器（观感优化 P1-1 路线 A）**：`FencedCode` 从 mark 装饰改为逐行 line 装饰拼容器——圆角、描边、整行背景（含行内空白处，mark 贴字问题结构性消除）、`#F6F7F8` 头部条；光标进块即撤容器回原文编辑（Typora 行为）。暗色实色分层。
- **代码块头部条 + 复制按钮（P1-2）**：`` ```js `` 的语言标签渲染成「语言标签 + 复制」widget；复制走 `navigator.clipboard`（Tauri 安全上下文实测可用）+ `execCommand` 兜底，**零插件依赖**，成功反馈"已复制"。
- **列表层级圆点（P0-5）**：光标不在项内时 `-`/`*` 折叠成 `•/◦/▪` 三级圆点（按 ListItem 祖先深度），光标进入恢复原文；有序列表保留数字 + `tabular-nums`。
- **大纲折叠树（P1-3）**：扁平列表 → 父子树渲染，`▸/▾` 折叠（状态按 `level:text` 记忆，防 500ms 防抖重建后弹开）、根节点=文档名（点击回文首）、条目两行截断 + 层级缩进引导线、面板 260px。
- **顶栏图标化（P0-8）**：文字按钮 → 无边框 inline SVG 图标（hover 出底色），文件名顶部居中，自动保存/主题收进「更多」下拉菜单（浏览器模式自动隐藏自动保存项）。
- **排版标定（P0 系列）**：标题间距 margin→padding（**修复点击定位漂移**——CM 行高测量不计 margin）；内容列 32px 留白 + 820px 栏宽（文本栏宽 ≈756px 基本不变）；亮色内容区纯白/暗色实色分层；行高 1.65；Segoe UI Variable + Cascadia Mono 字体栈；图片居中圆角 480px；行内代码 `2px 5px`/`.92em`；引用块浅背景。
- **表格真渲染（方案 A，v0.3.0）**：光标不在表内时整表 block widget 渲染成真 `<table>`（列对齐 `:---:`/`--:` 语法、表头底色、斑马纹、hover、宽表 `overflow-x` 内部横向滚动且不撑宽编辑器）；**单元格内行内 markdown 原地渲染**（`` `code` `` → 行内代码样式、`**粗**`/`*斜*`/`~~删~~` → 语义标签，反引号/星号不显示）；点击单元格 `posAtDOM` 反查源码位置进格编辑（光标离开自动恢复渲染）。整表 block 装饰走 StateField（CM6 硬限制：block 装饰禁经 ViewPlugin）+ `atomicRanges` 原子化 + 语法树渐进解析追踪（新载入文档即渲染）。
- **安装器中文化（v0.3.1）**：NSIS 向导编入简体中文语言包、按 Windows 系统语言自动匹配（不弹选择框）；MSI 界面语言设为 zh-CN（WiX WixUIExtension 自带中文资源）。
- **一键排版整理（T-4，v0.4.0）**：`Ctrl+Shift+T` / 「更多 → 排版整理」，中英/中数交界补空格 + 半角标点转全角。纯函数 `tidy.js`（`node --test` 单测），保护区间从语法树收集（代码块/行内码/链接/图片/URL/HTML），标题序号 `4.`、文件名 `index.md`、版本号 `1.2.3`、省略号 `...` 均不误伤；改动处数现场计数、`Ctrl+Z` 一步撤销。
- **实验室（T-2/T-3/T-5，v0.4.0）**：「更多 → 实验室」子菜单，`settings.lab` 持久化，模块级标志 + `labVersion` 驱动装饰层重建。含 `.txt` 纯文本即读（按文件名切 `plainExtensions` 扩展集，默认开）、`^^字^^` 着重号（CSS `text-emphasis`，默认关）。
- **链接视觉分类 + 编码提示（T-1/T-6，v0.4.0）**：文件链接虚线下划线、网页链接实线；GBK 猜测打开时 toast 告知「按 GBK 打开（保存保持原编码）」。
- **工程配套（M-3 等，v0.4.0）**：`npm run lint`（`scripts/lint.mjs` 对 src/test/scripts 全量 `node --check`，零依赖）、`npm test`（`node --test`）、仓库根 `CHANGELOG.md`（用户向一行一条）。

### 版本沿革摘要

版本 **0.4.1**（0.1.0 → 0.2.0：大纲/拖放/草稿/编码/图片/大纲侧栏/两轮审查修复；0.2.0 → 0.2.1：链接打开防命令注入、浏览器草稿恢复判脏、UTF-16 保存损坏修复、保存重入保护、图片缓存按文档隔离、Rust 单元测试；0.2.1 → 0.2.2：失败图缓存不再反复重试、启动 open-file 竞态缓冲、URL 白名单大小写对齐、图片 title 解析、拖放图 2MB 守卫、loadFile 串行化；0.2.2 → 0.2.3：**远程 http(s) 图片直接加载**（此前被误当本地路径解析必 broken）、CSP img-src 放行 http:/https:；0.2.3 → 0.3.0：**观感优化轮**（代码块容器/列表圆点/大纲折叠树/图标顶栏）+ **表格真渲染**（方案 A block widget）；0.3.0 → 0.3.1：**单元格行内渲染** + 阅读位置记忆 + 安装器中文化；0.3.1 → 0.4.0：**借鉴 Telari 第一轮**——一键排版整理（T-4）+ 实验室页（T-2/T-3/T-5）+ 链接分类与编码提示（T-1/T-6）+ 工程配套 lint/test/changelog（M-3），零新依赖；0.4.0 → 0.4.1：**表格补行翻转修复**——表末行 Enter 补新行全程保持编辑态（sticky 表下空行豁免，点击/方向键仍正常渲染），零新依赖）。

### 待扩展 / 已知边界

- 表格（v0.3.0 已落地方案 A：真表格渲染 + 行内 markdown + 点击进格）：剩余增强为**单元格内原地编辑**（CM 文本流列宽无法对齐，属架构边界）、**行列增删工具条**（widget 与 cell 反查逻辑可直接复用，工作量约 1.5 倍）、单元格内图片渲染（当前跳过）。
- **双栏预览模式、KaTeX / Mermaid**：需要引入 markdown-it/KaTeX/Mermaid 渲染栈，与「lite」体积定位有张力。**已定方向**：借鉴 Telari「设置 → 实验室」实验开关页作为承载容器，依赖动态 `import()` 懒加载、默认关闭（详见第 6 节 T-2）。**容器已就绪**（v0.4.0 实验室页 + `setLabFlags`），T-7/T-8 只差各自依赖，可后续单独引入挂到开关后。
- **代码签名与 SmartScreen**：面向分发需要代码签名证书（硬成本）。
- 加固暂缓项：IPC 读写路径白名单校验（CSP 已收紧；当前 IPC 威胁模型为"无远程内容、无 XSS 面"）。

## 3. 桌面集成详解（.md 关联 / 双击打开 / 右键新建）

**全自动、免管理员、换机即用**——不存在任何带绝对路径的手工注册表文件：

- **双击打开**：`tauri.conf.json` 的 `bundle.fileAssociations` 让安装器按实际安装路径注册 `.md`/`.markdown` 默认关联（ProgId `md`、`shell\open\command = "<exe>" "%1"`）。装完双击即在**同一窗口**打开（单实例 + `open-file` 事件转发），标题栏显示当前文件名。
- **右键"新建 → Markdown Document" + "打开方式"登记**：App 首次启动时 `setup()` 用 `winreg` 按 `current_exe()` 实际路径往 `HKCU\Software\Classes` 写 `<ext>\ShellNew`(NullFile) 与自建 ProgId `Typora-Lite.md`，**刻意不覆盖默认关联**（避免每次启动抢回用户改过的默认编辑器）。全部幂等、每次启动自愈（exe 挪位置后路径自动修正）。
- 若右键"新建"没立刻出现，是资源管理器缓存了子菜单，刷新即可：`ie4uinit.exe -show` 或重启 explorer。
- **卸载自动清理**：NSIS 卸载钩子（`installer-hooks.nsh` 的 `NSIS_HOOK_POSTUNINSTALL`）删除 App 自注册的 `Typora-Lite.md` ProgId、`.md`/`.markdown` 的 ShellNew 值与 `OpenWithProgids` 登记值；`OpenWithProgids` 里**其他应用**的 ProgId（形如 `xxxMD.*`）只删自己的值、键用 `/ifempty` 保留。
- **绝对不要手工写 .reg 导入关联**：.reg 不会展开环境变量，硬编码的 `C:\Users\<用户名>\...` 换台机器就是指向不存在的程序（实测踩坑）。手动改关联的正确姿势：右键 .md → 打开方式 → 选择其他应用 → Typora-Lite → 勾选"始终"。

## 4. 观感优化路线（v0.3.0 规划，✅ 已于 2026-09-15 全量落地）

> **落地状态**：L0 九项 + P1 三项全部完成并经真机 11/11 CDP 断言验证（明暗两套截图），施工细节与踩坑见 [开发日志](开发日志.md) 第 16 节。

完整方案见 [`观感优化方案.md`](观感优化方案.md)（差距清单、装饰模型分析、L0/L1/L2 分层、逐项验证标准均已核实到源码行级）。此处只记**已拍板的决策与对方案的补充修正**：

**已拍板**
- **代码块容器走路线 A**（逐行 line 装饰拼容器）：不碰 `caretIn` 驱动的"聚焦即编辑"模型，改动小、观感约 80%；A 的 CSS（`cm-cb` 系列）将来可整体复用到 B 路线的 block widget 上，**不存在返工**。
- **标题—正文间距先只动 `line-height` 一个旋钮**（1.7 → 1.65），截图对比后再决定是否继续收紧；一次改多个旋钮无法归因。
- **不做「阅读/编辑模式」开关**（方案第 3.5 节）：项目的成本中心在状态机（v0.2.0→0.2.3 四轮评审修的基本全是状态竞态），加正交状态位 = 测试面翻倍。代码块沿用 `caretIn` 同构的"光标进块即编辑"。

**对方案的补充修正（7 条）**
1. **P0-3（标题 margin → padding）提为第一优先级**：它不是观感项而是正确性修复——margin 不计入 CM 的行高测量，点击定位现在已在漂移。最先做、单独提交。
2. **路线 A 的技术红利**：`` ```js `` 首行的 `CodeInfo` 本来就被 `HIDE_MARKS` 折叠成空行，这行空行正好是**头部条的天然落点**——加 `cm-cb-first` + `::after` 即可画语言标签，几乎零成本；复制按钮可先做成语言标签旁的行内 widget，右对齐后置到 P1-2。
3. **剪贴板先给结论（已实测）**：真机 origin 为 `http://tauri.localhost` 但 `window.isSecureContext === true`（Tauri 2 已注册安全上下文），async Clipboard API 直接可用。实现用 `navigator.clipboard` → `execCommand('copy')` 双路径兜底，**零插件依赖**。
4. **暗色主题同步进 L0**：P0-6 只写了亮色（内容区 `#FFFFFF`）；暗色变量须一起调——代码块容器色从半透明 `rgba` 换成实色分层（头部条/代码区两档），否则白底+半透明容器的对比逻辑在暗色下反向翻车。验收标准明确写"明暗两套截图"。
5. **P0-1 的安全论证**：`max-width` 760→820 加左右 32px padding 后，实际文本栏宽 ≈756px 与现在几乎一致——即"加留白但不改行长"。
6. **P1-3 大纲折叠防坑**：`rebuildOutline` 每次全量重建 DOM，折叠状态必须存在 JS 里（按 `level+text` 做 key 的 Map），否则打字 500ms 防抖触发重建后折叠全弹开。
7. **验证手段升级**：方案第 5 节"像素断言"（代码块同行空白处背景色恒等于容器底色）用 Playwright 写成自动化截图+取色脚本，而非手工截图——浏览器兜底模式 `npm run dev` 即可跑视觉回归，无需打包 Tauri。

**落地顺序**（方案第 7 节基础上微调）：P0-3 → P0-1+P0-6（一起验证明暗两套）→ 其余 L0（每项单独提交，可二分回退）→ P1-1 路线 A（顺手用 `CodeInfo` 空行做头部条+语言标签）→ P1-2 / P1-3。

## 5. 版本路线与轮次记录

前端已通过 `npm run dev`（vite）在浏览器实测渲染无误。原生壳层已跑通：装好 **VS Build Tools 2022（C++ 工作负载，含 MSVC `link.exe` + Windows SDK）** 后，`npm run tauri build` 成功产出 `bundle/` 下的 `.msi` 与 nsis `-setup.exe`。最新交付的安装包见 [`installer` 分支](https://github.com/shenxinduwang/typora-lite/tree/installer)。逐轮踩坑与解法的完整记录见 [开发日志](开发日志.md)。

**2026-09-13 数据安全/编码/性能修复轮**（详见 [开发日志](开发日志.md) 第 8 节，全部真机验证）：关窗未保存拦截（取消/放弃/保存并退出三路实测）、Ctrl+Z 跨文件回退修复、GBK/UTF-8 BOM/UTF-16 识别与按原编码回写（iconv 造文件 + 回读字节级验证）、原子保存、打开/保存失败弹错、单实例转发竞态兜底、每键 IPC/全文字符串比较性能优化、选区主题色、分割线渲染、`core:window:allow-destroy` 权限补授、vite watch EBUSY 修复。

**2026-09-13 功能轮**（详见第 9 节）：查找/替换（Ctrl+F，全部匹配高亮）、代码块按语言着色、列表手感（内建回车续写 + 自研 Tab/Shift+Tab 缩进）、拖放 .md 打开、大纲侧栏（目录树/点击跳转/跟随光标高亮）。均经浏览器断言 + 安装版真机截图验证。

**2026-09-13 跨机部署修复**（详见第 10 节）：删除硬编码个人路径的 `md-association.reg`（跨机导入会指向不存在的程序）；新增 NSIS 卸载钩子 `installer-hooks.nsh` 清理运行时自注册键与悬挂默认值（其他应用登记值保留），本机完成"装→卸→清理→重装"全闭环验证。桌面集成现在是真正的**全自动、免管理员、换机即用、卸载干净**。

**2026-09-13 待扩展功能轮**（详见第 11 节）：手动主题切换（自动/亮/暗，持久化）、最近文件 + 启动恢复上次文档、崩溃草稿恢复（3s 草稿 + 编码/行尾随存）、可开关的 30s 自动保存、外部文件修改监听（mtime 轮询，干净静默重载/脏时确认）、图片粘贴与拖入（存 `assets/` 插相对引用）、CSP 收紧。浏览器断言 + 安装版真机逐项验证（含外部追加行自动重载、剪贴板图片落盘 assets/、无参数重启恢复）。

**2026-09-13 双报告复核修复轮（v0.2.0）**（详见第 12 节）：修复外部审查确认的 P0×2（草稿恢复判"干净"致二次丢数据；GBK 存 emoji 写成 `&#128512;`，现弹窗引导改存 UTF-8）、P1×3（保存/mtime 竞态、mtime 失败永久关监听、恢复草稿吞启动文件）、渲染补齐×5（CodeInfo/Setext/链接折叠/表格分隔行/**图片预览**）+ Ctrl+Click 开链接 + 暗色面板 + P3 批量优化。渲染组经浏览器断言验证，P0-2 与图片预览经安装版真机验证。

**2026-09-13 v0.2.1 复审修复轮**（详见第 13 节）：单元测试抓出 **UTF-16 文件保存必损坏**（`encoding_rs::encode()` 对 UTF-16 标签返回 UTF-8 字节，已改手动编码码元，往返单测守卫）；`open_external` 改 `rundll32` 杜绝 URL 经 `cmd` 的命令注入；浏览器分支草稿恢复判脏（补上 P0-1 修漏的同类项）；`selfWriting` 释放时机后移 + 保存重入保护；图片缓存按文档路径隔离（跨文档同名 rel 不再串图，外部改图能刷新）；浏览器 Ctrl+Click 兜底 `window.open`。`cargo test` 5/5 通过，vite/cargo 构建全绿。

**2026-09-13 v0.2.2 二次评审修复轮**（详见第 14 节）：失败图片缓存改 `has()` 判存（不再每次重绘重发注定失败的 IPC）；启动期 open-file 事件缓冲（崩溃恢复确认期间到达的载入不再被恢复分支静默覆盖）；`is_allowed_url` 大小写归一（`HTTP://` 链接不再被 Rust 端误拒）；`![alt](src "title")` 的 src 正确剥离 title；浏览器拖放图片复用 2MB data URL 守卫；`loadFile` promise 链串行化（连开请求不再互相覆盖）。修复经 code-reviewer 子代理逐行核验无回归；`cargo test` 5/5、构建全绿，0.2.2 双安装包已入交付目录。

**2026-09-13 v0.2.2 真机验证轮**：0.2.2 静默安装升级成功；脚本驱动 GUI + 字节级断言全部通过——UTF-16LE 保存往返无损（`ff fe` + 合法码元，无旧 bug 特征）、GBK 粘 emoji 转存 UTF-8 全流程（确认框 Enter 后 `f0 9f 98 80` 完整落盘）、强杀崩溃 → 草稿恢复 → 恢复内容判脏（标题 `•`）、干净退出/两步放弃退出/无参数恢复最近文档均正常。本机已装 0.2.2。

**2026-09-14 v0.2.3 网络图片修复轮**（详见第 15 节）：外部评审发现知乎等外部文档的 `https://` 远程图片全部 broken——`toDOM` 把带协议 URL 误当本地路径丢给 `read_asset_base64` 必失败。修复：远程 src 直接交给 WebView 加载 + CSP `img-src` 放行 `http:/https:`。真机经 WebView2 CDP 远程调试断言：本地 HTTP 服务图命中服务端日志、知乎图加载成功、**用户真实知乎文档 19 个图片实例 19 ok / 0 broken**、相对路径本地图片回归通过。

**2026-09-15 观感优化轮（L0 全量 + P1 路线 A）**（详见第 16 节，方案见 [`观感优化方案.md`](观感优化方案.md)）：代码块真容器（逐行 line 装饰，圆角/描边/头部条/复制按钮，光标进块即编辑）、列表 `•/◦/▪` 层级圆点、大纲折叠树、顶栏 SVG 图标化 + 更多菜单、标题 margin→padding **修复点击定位漂移**、内容列留白/区域色明暗重排、字体栈与行高标定。真机 NSIS 静默安装 + CDP 断言 **11/11 全绿**（容器整行背景像素断言、剪贴板真机实测、•/◦/▪ 三级、折叠 2→1、主题循环 dark），原文档无痕恢复。施工中抓出 BulletWidget off-by-one（浏览器实测 8 个全 `•`）与 `newFile()` 空文档两坑并修正。

**2026-09-15 v0.3.0 表格真渲染轮（方案 A）**（详见第 17 节）：「待扩展」头条落地——光标不在表内时整表 block widget 渲染成真 `<table>`（列对齐/表头底色/斑马纹/hover/宽表内部横向滚动），点击单元格反查源码位置进格编辑；整表 block 装饰走 StateField + `atomicRanges`。施工中连修 5 个 CM6 深水区坑（atomicRanges facet 形态、block 装饰禁经 ViewPlugin、facet 纯值下发、`set.between` 崩溃、flex 布局下宽表撑宽编辑器）。浏览器 8/8 + 真机 9/9 断言全绿（含宽表滚动与编辑器宽度断言、明暗截图），用户文档无痕恢复。

**2026-09-15 v0.3.1 行内渲染修复 + 阅读位置记忆 + 安装器中文化**（详见第 17/18 节）：用户实测反馈表格内反引号原样显示——修复为单元格行内 markdown 渲染（code/em/strong/del，反引号不再泄漏），并连带发现修复「新载入文档表格不渲染」bug（StateField 未追踪语法树渐进解析）。新增**阅读位置记忆**（重开文档自动跳回上次位置 + 轻提示）与**安装器中文化**（NSIS 简体向导按系统语言自动匹配、MSI 界面 zh-CN）。真机断言：行内渲染 10/10、位置恢复（toast + 滚动精度）通过。

**2026-09-18 v0.4.0 借鉴 Telari 第一轮（零新依赖）**（详见第 19 节，方案见第 6 节）：解包对标 macOS 原生阅读器 Telari（`dev.telari.reader`，实为 SwiftUI + Rust pulldown-cmark，与本项目零代码交集、定位互补）后落地其低成本可迁移项——**一键排版整理**（`Ctrl+Shift+T`，中英混排补空格 + 半角→全角标点，语法树保护区掩码，`4.`/`index.md`/`1.2.3`/`...` 不误伤）、**实验室页**（`settings.lab` 持久化 + `labVersion` 驱动装饰重建，含 `.txt` 纯文本即读、`^^字^^` 着重号）、**链接虚/实线分类** + **GBK 猜测编码提示**。工程配套 `npm run lint`（`scripts/lint.mjs`）/ `npm test`（`node --test`，9 例）/ `CHANGELOG.md`（M-3），零新依赖。T-7/T-8（Mermaid/KaTeX）因需新依赖挂后，T-9 打印因 CM6 视口虚拟化延后单独一轮。`node --test` 9/9、`cargo test` 5/5（Rust 未改）、vite build 全绿，浏览器 CDP 断言排版整理 + 着重号 + 链接分类通过。

**2026-09-18 v0.4.1 表格补行翻转修复（补丁轮）**（详见第 20 节）：用户实测反馈「在做好的表格中编辑会反复进入阅读和编辑模式」——复现定位为**最后一行按 Enter 补新行**时光标瞬时在 Table 节点外→widget 渲染→敲 `|` 又吞回，每行弹一次。修复为 sticky 豁免：`tableEdgeSticky` StateField 只在编辑事务（`tr.docChanged`）把光标带到表下紧邻空行时授予豁免，任何纯选择变化即清除；点击/方向键停到该空行仍正常渲染。StateField 与 ViewPlugin 两层装饰共用同一谓词，字段注册顺序（sticky 先于 tableWidgets）为硬约束。安装版真机验证：补行全程编辑态不翻转，IME 合成/光标移动/点击循环零异常，0.4.0 特性回归 22/22，lint 8/8，`node --test` 9/9，零新依赖。

## 6. 借鉴 Telari：思路与功能路线（2026-09-18 规划；✅ 第一轮 T-1~T-6 + M-3 已于同日 v0.4.0 落地，详见 [开发日志](开发日志.md) 第 19 节）

> 背景：对标的 Telari（macOS 原生 Markdown **阅读器**，v0.5.6，`dev.telari.reader`）经解包核实为 SwiftUI/AppKit/CoreText + Rust 核心（pulldown-cmark + syntect/two-face + dagre 布局 + 自研 LaTeX 排版，Quick Look 扩展 + Sparkle 自动更新），与本项目（Tauri + CM6 **编辑器**）零代码交集、定位互补。其 changelog（2026-06-28 → 09-18 共 38 版）的方法论与功能清单是低成本的取经来源。

### 6.1 方法论借鉴（流程类，不涉功能代码）

- **M-1 高频原子小版本**：Telari 一天一版、每版一个用户可见主题（Mermaid 轮/中文标点轮/设置面板轮），与本项目「轮次」制同构。落地：改完即发 patch 版，不再攒大版本。
- **M-2 自动更新通道**：Telari 发版密的前提是 Sparkle 静默升级。落地：接入 `tauri-plugin-updater`（需静态文件托管 + 签名密钥），是 M-1 的前置投资。**注意**：updater 签名密钥与「代码签名证书」是两回事，前者免费。
- **M-3 用户向 changelog 页**：把开发日志各轮标题提炼成面向用户的一行一条。落地：官网/仓库 `CHANGELOG.md`，随每次发版更新。✅ 已建（v0.4.0 起随版更新）。
- **M-4 生成物测试钉死**：Telari 的 `SYNTAX-ACKNOWLEDGEMENTS.md` 由测试逐字节 pin 防漂移。落地：THIRD-PARTY-NOTICES.txt 类文件同样加校验守卫。
- **M-5 阅读位置回归清单**：Telari 0.5.x 连修四类位置漂移（外部修改热重载错行、窗口反复缩放位置下爬、源码视图改设置回原行），印证本项目 v0.3.1 防抖串写 bug 的教训——**滚动位置保持是最易回归的不变量**。落地：把「外部修改热重载保位置 / 窗口缩放保位置 / 切设置保位置」写成 CDP 断言用例。

### 6.2 功能借鉴（按成本分层，T-1 起为建议施工顺序）

**零/低成本级**

- **T-1 编码猜测提示**（Telari 0.5.4）：非 UTF-8 文件用 `encoding_rs` 猜测成功后，toast「已按 GB18030 打开（编码为猜测）」。仅 UI 提示，Rust 侧信息已具备。✅ v0.4.0：仅 GBK 回退触发（BOM/严格 UTF-8 是确证不该提示），文案注明保存保持原编码。
- **T-2 「设置 → 实验室」页**（0.5.4）：设置面板新增实验页，作为一切可选增强的容器（解 2 节「待扩展」KaTeX/Mermaid 的定位张力）。本身只是设置项 + 持久化。✅ v0.4.0：「更多 → 实验室」子菜单，`settings.lab` 持久化，`setLabFlags` + 版本号 bump 驱动装饰层重建。
- **T-3 `.txt` 打开即读**（0.5.4）：文件关联加 `.txt`，载入时跳过 liveMarkdown 装饰直接纯文本渲染。✅ v0.4.0（实验室开关，默认开）：按文件名推导扩展集（`plainExtensions`），浏览器打开/拖放同样生效。**有意偏离**：不注册 `.txt` 系统文件关联——会把全系统 .txt 默认打开方式抢来，只放行应用内打开/拖放。
- **T-4 中英混排自动空格**（0.4.2）：命令式「一键整理混排间距」，纯正则替换（保留代码区/URL 豁免）；顺带做「半角→全角标点」修正，**必抄 Telari 守卫**：标题序号 `4.` 不得被纠成 `4。`（0.5.3 踩坑）。✅ v0.4.0：`Ctrl+Shift+T`，纯函数 `tidy.js` + 语法树保护区掩码，改动处数现场计数、toast 反馈、一步撤销。
- **T-5 着重号渲染**（0.3.5）：`**字**` 之外的 GFM 无此语法，可用 `^^字^^` 或 HTML `<em>` 映射到 CSS `text-emphasis`——WebView 原生支持，比 Telari 自绘便宜得多。✅ v0.4.0（实验室开关，默认关）：`^^字^^` → CSS `text-emphasis: filled dot` 字下圆点，定界符折叠。

**小功能级**

- **T-6 链接视觉分类**（0.5.0）：文件链接 vs 网页链接下划线样式区分（虚线/实线）；`mailto:` 等 scheme 已在 Ctrl+Click 白名单，补渲染态提示。✅ v0.4.0：文件链接（相对路径/`\` 开头的绝对路径以外的非 web href）虚线下划线，`http(s)/mailto/data:` 实线。
- **T-7 Mermaid 实验支持**（0.5.4，挂 T-2 开关后）：fenced code ` ```mermaid ` 检测，光标离开块时动态 `import()` mermaid 渲染成 SVG widget，光标进块回源码——完全复用代码块容器 `caretIn` 模型，不新增状态位。**待做**：受本轮"零新依赖"约束，挂 T-2 已就绪的实验室开关后引入。
- **T-8 KaTeX 实验支持**（同上）：`$...$`/`$$...$$` 懒加载渲染。注意 Telari 的坑单：超宽公式横向滚动与纵向滚动互卡（0.5.3）、「公式缩小适配」下窗口缩放位置下爬（0.5.3）、复制公式带回定界符（0.5.0）。**待做**：同 T-7，需 KaTeX 依赖。
- **T-9 打印/导出 PDF**：本项目空白区。WebView `window.print()` + 打印 CSS 即白送 Telari 0.4.10 的「跨页表格每页重复表头」（`thead{display:table-header-group}` 一行）。**延期**：CM6 视口虚拟化下 `window.print()` 只打印可见行，需先做"全文离屏渲染"管线，非本轮小改，单列一轮。

**中功能级 / 测试清单级**

- **T-10 图片显示规则回归表**（0.5.3）：把 Telari 修过的图片场景做成 `liveMarkdown.js` 用例清单——列表项内的图、连排多图、段中图、带 title 的图、**Windows 反斜杠相对路径**（Telari 修的就是这个，本项目原生 Windows 更需自查）。
- **T-11 脚注支持**（0.5.0）：`[^1]` 渲染折叠 + **按首次引用顺序重编号**（Telari 0.5.0 语义）；lezer-markdown 需 GFM 扩展或自写 walk。
- **T-12 大文件策略**（0.4.12/0.5.4）：几十 MB 文档「打开即读」——超阈值自动降级（关装饰、只保留纯文本滚动），阈值内保持全功能。

**明确不抄**：Quick Look/Finder 集成（macOS 独有）；多文件小说连读、工作区符号链接侧栏（阅读器/文件管理器形态，与编辑器定位冲突）；原生 CoreText 排版路线（架构级换轨，不评估）。

### 6.3 核心结论

Telari 证明「lite」不靠砍功能维持，靠 **实验区（T-2）+ 懒加载（T-7/T-8）+ 高频小版本（M-1/M-2）** 维持。本项目「待扩展」里挂着的 KaTeX/Mermaid 张力问题，用实验室页这个容器即可收编，不再需要做"是否违背 lite 定位"的逐项拉扯。

