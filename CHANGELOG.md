# 更新日志

面向用户的一行一条速览（借鉴 Telari 的公开 changelog 形态）。逐轮施工细节见 [docs/开发日志.md](docs/开发日志.md)。

## 0.4.1（2026-09-18）—— 表格补行不再来回切换阅读/编辑态

- **修复**：在已渲染表格的最后一行按 `Enter` 新增行时，整表会立刻跳回渲染态、敲出 `|` 又跳回源码态，每加一行来回弹一次。现在「编辑动作把光标带到表格紧邻下方空行」按仍在表内处理，补行全程保持源码编辑；点击/方向键停到该空行仍正常渲染，不影响阅读观感。

## 0.4.0（2026-09-18）—— 借鉴 Telari 第一轮：排版整理 + 实验室

- **一键排版整理**（`Ctrl+Shift+T` / 「更多 → 排版整理」）：中英文/数字交界自动补空格、半角标点转全角；代码块、行内代码、链接、URL、HTML 全程豁免，标题序号 `4.` 不会被误转 `4。`；改动处数即时反馈，`Ctrl+Z` 一步撤销。
- **设置 → 实验室**（「更多 → 实验室」子菜单）：实验性功能统一开关容器，选择持久化，为后续 Mermaid/KaTeX 等懒加载增强预留位置。
- **`.txt` 纯文本阅读**（实验室，默认开）：打开 `.txt` 按纯文本显示，不套 Markdown 渲染，读几十 MB 文本也不再解析装饰。
- **着重号渲染**（实验室，默认关）：`^^文字^^` 在光标离开后渲染为字下圆点着重号，进入即恢复源码。
- **链接视觉分类**：文件链接（相对/绝对路径）虚线下划线，网页链接实线下划线，一眼分辨点击去向。
- **编码猜测提示**：按 GBK 猜测打开的非 UTF-8 文件给出轻提示「已按 GBK 打开（保存时保持原编码）」，不再静默猜编码。
- 工程配套：`npm run lint`（`node --check` 全量语法检查，零新依赖）、`npm test`（`node --test` 纯函数单测）。

## 历史摘要

- **0.3.1**：表格单元格行内 markdown 渲染、阅读位置记忆、安装器中文化（NSIS/MSI 简体中文）。
- **0.3.0**：表格真渲染（列对齐/斑马纹/宽表内部滚动/点击进格编辑）；观感优化轮（代码块容器+复制按钮、列表层级圆点、大纲折叠树、顶栏图标化）。
- **0.2.3**：远程 `http(s)` 图片直接加载（修复知乎等外链图全 broken）。
- **0.2.2**：启动 open-file 竞态缓冲、loadFile 串行化、失败图缓存判存、URL 白名单大小写对齐等二次评审收口。
- **0.2.1**：UTF-16 保存损坏修复、`open_external` 防命令注入、图片缓存按文档隔离、`cargo test` 上线。
- **0.2.0**：双报告复核修复轮——草稿恢复判脏、GBK 存 emoji 保护、链接折叠 + Ctrl+Click、图片预览、暗色面板。
- **0.1.x**：编码识别（UTF-8/BOM/UTF-16/GBK）与按原编码回写、原子保存、最近文件、崩溃草稿、自动保存、外部修改监听、图片粘贴/拖入、查找替换、大纲侧栏、主题切换。
